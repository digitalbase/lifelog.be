import React, { FunctionComponent } from 'react';

const Footer: FunctionComponent = () => {

    return (
        <footer>
            I am a footer
        </footer>
    );
};

export default Footer;
